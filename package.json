
{
  "name": "kombajn-app",
  "version": "1.0.0",
  "private": true,
  "description": "Teamwork Kombajn Application",
  "scripts": {
    "build": "mkdir -p dist && esbuild index.ts --bundle --outfile=dist/index.js --platform=browser --target=es2020 --loader:.ts=ts --external:@google/genai --external:@supabase/supabase-js && cp index.html index.css dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^0.14.0",
    "@supabase/supabase-js": "^2.50.3",
    "@vercel/node": "^5.3.4",
    "esbuild": "^0.20.2"
  },
  "devDependencies": {
  }
}